services:
  - type: web
    name: ai-roi-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./backend
    region: oregon
    plan: free
    branch: main
    healthCheckPath: /health
    buildCommand: ""
    envVars:
      - key: PORT
        value: 8000
    autoDeploy: true
